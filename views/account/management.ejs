<%# Content Heading %>

<% if (title) { %>
<h1><%= title %></h1>
<% } else {
  res.redirect('/')
} %>

<%# Flash Message %>

<%- messages() %>

<%# Errors %>

<% if (errors) { %>
  <ul class="notice">
 <% errors.array().forEach(error => { %>
   <li><%= error.msg %></li>
<%  }) %>
 </ul>
<% } %>

<% if (accountData.account_type === "Admin" || accountData.account_type === "Employee") { %>

  <div id="welcome-admin">
    <h2>Welcome <%= accountData.account_firstname %></h2>
    <h3>You're logged in.</h3>
    <p id="manage-account--link"><a href="">Edit Account Information</a></p>
    <h2 id="inv-management">Inventory Management</h2>
    <p id="manage-inv--link"><a href="/inv/management">Manage Inventory</a></p>
  </div>
  

<% } else { %>
  
  <div id="welcome-client">
    <h2>Welcome <%= accountData.account_firstname %></h2>
    <h3>You're logged in.</h3>
    <p id="manage-account--link"><a href="">Edit Account Information</a></p>
  </div>
  

<% } %>